<UserControl x:Class="UntisDesktop.UserControls.TimegridDay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ViewModels="clr-namespace:UntisDesktop.ViewModels"
             xmlns:Localization="clr-namespace:UntisDesktop.Localization"
             mc:Ignorable="d">
    <UserControl.DataContext>
        <ViewModels:TimegridDayViewModel/>
    </UserControl.DataContext>
    <Border BorderBrush="Black" BorderThickness="1,0.5,1,0.5">
        <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <TextBlock Text="{Binding Date, StringFormat=\{0:M\}, ConverterCulture={x:Static Localization:LangHelper.Culture}, Mode=OneWay}" FontSize="14" Grid.Row="0" HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding LocalizedDayName, Mode=OneWay}" FontSize="14" Grid.Row="1" HorizontalAlignment="Center"/>
        </Grid>
    </Border>
</UserControl>
