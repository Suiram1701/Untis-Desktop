<UserControl x:Class="UntisDesktop.UserControls.TimegridHour"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Localization="clr-namespace:UntisDesktop.Localization"
             mc:Ignorable="d"
             DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Border BorderBrush="Black" BorderThickness="0.5,1,0.5,1" Padding="4,0">
        <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <TextBlock Text="{Binding HourName, Mode=OneTime}" FontSize="14" Grid.Row="0" HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding Length, Mode=OneTime}" FontSize="14" Grid.Row="1" HorizontalAlignment="Center"/>
        </Grid>
    </Border>
</UserControl>
