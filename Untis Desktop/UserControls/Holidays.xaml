<UserControl x:Class="UntisDesktop.UserControls.Holidays"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             DataContext="{Binding RelativeSource={RelativeSource Self}}" Margin="2">
    <Border CornerRadius="10" Background="#067ee7">
        <TextBlock Name="Text" Text="{Binding HolidayName, Mode=OneTime}" FontSize="16" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock.LayoutTransform>
                <RotateTransform Angle="90"
                                 CenterY="{Binding ElementName=Text, Path=ActualHeight, Converter={StaticResource MathConverter}, ConverterParameter=/2}"
                                 CenterX="{Binding ElementName=Text, Path=ActualWidth, Converter={StaticResource MathConverter}, ConverterParameter=/2}"/>
            </TextBlock.LayoutTransform>
        </TextBlock>
    </Border>
</UserControl>
